# frozen_string_literal: true

# Operator user is created in db/seeds.rb (idempotent).
class SeedMadcpOperatorUser < ActiveRecord::Migration[8.1]
  def change
    # no-op placeholder kept for migration order; see db/seeds.rb
  end
end
